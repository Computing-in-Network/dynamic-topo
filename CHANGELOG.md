# Changelog

## v0.2.0 - 2026-02-23

### Topology Backend
- 支持链路策略参数外置与热加载（`--link-policy` / `--hot-reload-link-policy`）。
- 链路稳定性增强：最短保持时长（`min_link_up_s` / `min_link_down_s`）与每 tick 翻转统计。
- 新增连通性统计：连通分量数量、最大分量规模/占比、直径近似。
- 引入增量几何重算框架（默认关闭，保留全量回退路径）。

### Frontend Visualization
- 新增图层控制面板（节点/链路/轨迹/轨道环/标签）与本地持久化。
- 新增节点/链路拾取高亮与详情侧栏。
- 新增运行状态告警（断流、延迟、低帧率）与健康徽章。
- 新增时间控制（暂停/单步/0.5x/1x/2x）与缓冲队列可视化。
- 300 节点场景渲染路径优化（位置复用、轨道节流、样式去冗余更新）。

### Quality & Process
- 新增固定种子快照基线与回归测试。
- 建立 Git Flow 回退标签规范（`rb-develop-*`）与脚本。
- 每个 feature 合并后打回退标签并在 issue 评论记录标签链接。
